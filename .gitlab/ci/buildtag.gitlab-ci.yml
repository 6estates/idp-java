buildTag:
  stage: buildtag
  dependencies: []
  script: 
    - time_cost buildtag -g fullstack -p idp-java --build-change-log true
  only:
    - /^release-v[0-9]+(?:.[0-9]+)+$/
    - /^hotfix-v[0-9]+(?:.[0-9]+)+$/
  except:
    - tags
  when: manual