test:
  stage: test
  image: sixhub-registry.ap-southeast-5.cr.aliyuncs.com/sixhub/mvnd-openjdk-11
  script:
    - mvn clean install
  only:
  - /^v[0-9]+(?:.[0-9]+)+$/
  - develop
  - /^release-v[0-9]+(?:.[0-9]+)+$/
